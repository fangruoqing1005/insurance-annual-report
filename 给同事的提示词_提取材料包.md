# 给同事的提示词（直接粘贴到她的 WorkBuddy 对话里发送即可）

---

请帮我整理一份「保险年报 AI 提取流程」的完整材料包。你不需要理解业务细节，只需要帮我**定位、阅读、打包**下面列出的文件，并生成一份说明文档。

## 背景

我这边需要复现一套流程：把保险公司年报 PDF 用 **MinerU（magic-pdf）** 转换成 Markdown 结构化文本，再用脚本按指标模板（约 163 行 × 17 列：公司名称/报告期/报表类型/指标编号/数值等）提取数据，最终生成 36 家公司的数据库（每家 163 行）。这套流程之前在这台电脑上跑通过，现在需要把所有相关材料整理出来发给同事。

## 任务清单（按顺序做）

**1. 查找提取脚本**
- 在以下位置搜索与"提取/年报/保险/minerU/magic-pdf/pdf"相关的脚本文件（`.py` / `.mjs` / `.js` / `.ipynb`）：
  - 桌面（`C:\Users\<当前用户>\Desktop\`）
  - 文档（`C:\Users\<当前用户>\Documents\` 及子目录）
  - 下载（`C:\Users\<当前用户>\Downloads\`）
  - 项目目录（`C:\Users\<当前用户>\WorkBuddy\` 下的各个项目文件夹）
  - `C:\Users\<当前用户>\.workbuddy\`（含 scripts 相关目录）
- 找到后**阅读脚本**，在说明文档里总结：输入是什么（PDF/Markdown 路径）、输出是什么（Excel/JSON）、依赖了哪些第三方库、关键函数/步骤。

**2. 查找 Skill**
- 查看 `C:\Users\<当前用户>\.workbuddy\skills\` 目录（用户级 skill）和各个项目目录下的 `.workbuddy\skills\`（项目级 skill）
- 凡是名称或内容（SKILL.md 首行描述）与"保险/年报/提取/PDF/财务/表格"相关的，整个文件夹列入材料包

**3. 确认 MinerU 使用情况**
- 运行 `pip list` 或 `pip show magic-pdf` 确认是否安装、版本号
- 在用户目录和常见配置位置查找 MinerU 相关配置（`magic-pdf` 配置文件、`~/.magic-pdf` 目录等）
- 如果查不到，如实说明"未找到本地安装记录"（可能是 API 方式调用）

**4. 查找数据文件（多年度的）**
- 在桌面/文档/项目目录搜索：`*.xlsx` / `*.json` / `*.csv`，文件名或内容含"年报/数据库/提取/指标/定位表"的文件
- 特别是多个年度（2023/2024/2025）的数据文件，一并列出路径和大致行数

**5. 查找历史提示词/方法说明**
- 查看 `C:\Users\<当前用户>\.workbuddy\memory\` 下的记忆文件、以及项目 `.workbuddy\memory\` 日志，找出与"提取/年报/MinerU/模板"相关的记录
- 如果有保存过提取相关的提示词或流程说明，摘录进说明文档

**6. 打包**
- 把第 1-5 步找到的所有**相关文件**复制到一个新文件夹 `C:\Users\<当前用户>\Desktop\提取材料包_<今天日期>\`（**只复制，不要移动/删除原文件**）
- 生成一份 `说明文档.md` 放进该文件夹，内容包括：
  - 材料清单（文件路径 + 用途）
  - 提取脚本的逻辑摘要（输入/输出/依赖/步骤，尽量详细）
  - MinerU 版本与部署方式（如能找到）
  - 没有找到的材料（明确写"未找到"，不要编造）
- 最后把整个文件夹压缩成 `提取材料包_<今天日期>.zip` 放到桌面

## 注意事项

- 全程**只读和复制**，绝不修改、删除、移动任何原文件
- 找不到的东西如实说明，不要猜测或编造路径
- 如果某一步搜索超时或目录过大，缩小范围（按关键词过滤文件名即可，不必逐个读取所有文件内容）
- 完成后告诉我 zip 的完整路径

---

> 使用方法：把上面整段发给同事，她粘贴到自己的 WorkBuddy 里发送即可。运行完会得到桌面上的「提取材料包_日期.zip」+ 说明文档，直接转发给你即可。
