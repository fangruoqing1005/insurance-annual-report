# 中国人寿 2025 年度 AI 提取结果校验报告

**校验时间**：2026-08-31
**数据源**：云端 `/api/data`（AI 提取入库结果） vs 人工校对版 `国寿.xlsx`（163 行）
**校验工具**：`scripts/verify_extract.py`（可按公司/报告期复用于其他公司）

---

## 一、总体结论（严重问题）

| 维度 | 校对版 | 云端入库 | 结论 |
|---|---|---|---|
| 行数 | 163 | 23 | **覆盖严重不足（仅 14%）** |
| 报表覆盖 | T01-T10 全部 | 仅 T01(部分)、T02(部分) | T03-T10 全部缺失 |
| 期间覆盖 | 期末+期初 / 本期+上期 | 仅"本期末/本期" | **上期/期初全部缺失** |
| 数值正确性 | — | 23 项中 5 项差异 | 含 3 项严重错值 |

> ⚠️ **当前入库数据不可作为分析依据**，需修复提取流程后重新提取，或先清空后重跑。

---

## 二、全面性：缺失明细（134 项）

### 报表级缺失（整表缺失）
| 报表 | 指标 | 说明 |
|---|---|---|
| T03 保险合同负债余额调节表 | C01-C14 | 整表缺失 |
| T04 履约现金流和合同服务边际 | D01-D10 | 整表缺失 |
| T05 初始确认 | E01-E11 | 整表缺失 |
| T06 保险服务收入 | F01-F06 | 整表缺失 |
| T07 CSM 过渡 | G01-G03 | 整表缺失 |
| T08 业务及管理费 | H01-H03 | 整表缺失 |
| T09 折现率 | I01 | 整表缺失 |
| T10 非金融风险调整 | J01 | 整表缺失 |

### 表内部分缺失
- **T01**：A03（其他债权投资）缺失；全部"期初"期间缺失
- **T02**：B18-B25（8 个指标）缺失；全部"上期"期间缺失

### 期间缺失模式
- 所有"期初/上期"行未入库 → AI 模型输出缺"上期"列，或后端 `periodVal` 映射未取到值

---

## 三、准确性：共同 23 项的差异（5 项）

| 报表/指标 | 期间 | 校对值(百万元) | 云端值(百万元) | 问题 |
|---|---|---|---|---|
| T01 A02 债权投资 | 期末 | 173,992 | **3,926,042** | ❌ 错位：取成了"其他债权投资"的值（A02 与 A03 子串误匹配） |
| T01 A05 保险合同负债 | 期末 | 6,376,114 | **312** | ❌ 严重错值 |
| T01 A06 净资产 | 期末 | 608,393 | 608,393 | ⚠️ 值对，但来源命名 "净资产-期末" vs 校对 "股东权益合计-期末" |
| T01 A07 总资产 | 期末 | 7,591,004 | 7,591,004 | ⚠️ 值对，但来源命名 "总资产-期末" vs 校对 "资产总计-期末" |
| T02 B03 投资收益 | 本期 | 198,457 | **6,353** | ❌ 严重错值 |

其余 18 项（如 A01 交易性金融资产 2,067,288、A04 其他权益工具 317,876、B01 保险服务收入 214,136、B17 净利润 156,552 等）与校对版一致 ✓

---

## 四、根因分析

1. **覆盖不足（T03-T10 全空 + 表内缺项）**
   - 后端 `locateTable` 未定位到 T03-T10 的关键词 → 整表跳过（不报错、静默 0 行）
   - 前端批次循环对 rowsAdded=0 的批次显示为"提取完成"，用户无感知
   - 单页提取覆盖阈值 `>=60%` 提前 break，主表页提取不全时不再向后扩展

2. **单期间（缺上期/期初）**
   - `extractTable` 要求模型每行输出"本期"+"上期"，但模型对部分行只给了本期
   - `matchIndicators` 后 `periodVal` 对 null 值直接跳过 → 上期全丢

3. **数值错位（A02/A03 混淆）**
   - `rowNameMatches`/`matchIndicators` 中 "债权投资" 是 "其他债权投资" 的子串，`rn.includes(inN)` 打 1 分兜底 → 抢行错配，且 `_suspicious` 标记未阻止入库

4. **来源命名不规范**
   - 指标来源/关键词由代码拼 `指标名称-期间`，而校对版用披露原文（"股东权益合计"、"资产总计"）→ 后续"指标截图检索"可能定位失败

---

## 五、修复建议（按优先级）

| 优先级 | 事项 | 方案 |
|---|---|---|
| P0 | 修复 A02/A03 类子串误匹配 | `matchIndicators` 对 `includes` 打 1 分的候选标记 `_suspicious` 且**score<2 不入库**；匹配时优先全等/前缀，杜绝"债权投资"吃掉"其他债权投资" |
| P0 | 上期/期初全缺失 | 提示词强化"每行必须输出本期+上期两列，缺失填 null"；后端对 `rowsAdded` 为 0 的批次**报错而非静默** |
| P1 | T03-T10 未定位 | 扩充 `TABLE_KEYWORDS`（如 T04 增加"保险合同负债（继续受保）"等实际表头）；`locateTable` 未命中时返回诊断信息而非跳过 |
| P1 | 来源命名规范化 | 模板中"指标来源"改用披露原文（校对版为准），关键词保留检索用 |
| P2 | 用户感知 | 前端批次汇总显示"该批次 0 行"红色警告 + 建议重试 |

---

*校验脚本可复用：`python scripts/verify_extract.py <校对Excel> <云端data.json> <公司名> <报告期>`*
